dependencies: dotenv, body-parser (middleware for this application), express (wave framework)??, mongoose

Make sure you have the postman agent running to communicate the postman on your workspace to communicate with your localhost. Select the desktop agent on the bottom right corner of the site

Tech used:

- mongo db
- postman

To run the server: npm run dev

currently at 1:20:00
